# Fortify Web

React + Vite + TailwindCSS v4 + Zustand + TanStack Query

## Scripts

- `npm run dev` — http://localhost:5173
- `npm run build` — build produção

## Estrutura

```
src/
  app/        # router, layouts, providers
  features/   # domínios
  shared/     # ui, lib
  stores/     # Zustand
```

## Env

`VITE_API_URL=http://localhost:3001/api`
