# Fortify Web — variáveis de ambiente (prefixo VITE_ é obrigatório no Vite)
# Copie para web/.env:  Copy-Item .env.example .env   (PowerShell, dentro de web/)

# URL base da API (inclui /api)
VITE_API_URL=http://localhost:3001/api
