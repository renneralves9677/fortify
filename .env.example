# Fortify — este repositório NÃO usa .env na raiz em runtime.
#
# Configure os arquivos por pacote:
#   api/.env   ← copie de api/.env.example
#   web/.env   ← copie de web/.env.example
#
# O PostgreSQL local sobe via Docker Compose (docker-compose.yml na raiz).
